<div class="wechat-container">
    <h1>Connect on WeChat</h1>

    <!-- QR Code -->
    <div class="wechat-image-box">
        <img src="/images/wechat/wechat-qr.jpeg" alt="WeChat QR Code" />
    </div>

    <!-- WeChat ID -->
    <p class="wechat-id">WeChat ID: <span>{{wechatId}}</span></p>

    <!-- Back Button -->
    <a href="/" class="back-link" aria-label="Go back">« Back to Home</a>
</div>

<style>
    .wechat-container {
        max-width: 400px;
        margin: 60px auto;
        background: #fff;
        border-radius: 16px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.08);
        padding: 35px 25px;
        text-align: center;
        animation: fadeInUp 0.6s ease;
    }

    .wechat-container h1 {
        margin-bottom: 20px;
        color: #1AAD19;
        font-size: 1.8rem;
        font-weight: 600;
    }

    /* Image container */
    .wechat-image-box {
        width: 250px;
        height: 250px;
        margin: 0 auto 20px auto;
        border: 3px solid #1AAD19;
        border-radius: 12px;
        overflow: hidden;
        /* force image to stay inside */
    }

    .wechat-image-box img {
        width: 100%;
        height: 100%;
        object-fit: contain;
        /* perfect fill */
        transition: transform 0.3s ease;
    }

    .wechat-image-box img:hover {
        transform: scale(1.05);
    }

    .wechat-id {
        font-weight: 500;
        font-size: 1.1rem;
        color: #333;
        margin-bottom: 20px;
    }

    .wechat-id span {
        color: #1AAD19;
        font-weight: 600;
    }

    a.back-link {
        display: inline-block;
        margin-top: 10px;
        text-decoration: none;
        color: #1AAD19;
        font-weight: 600;
        border: 1.5px solid #1AAD19;
        padding: 10px 24px;
        border-radius: 8px;
        transition: all 0.3s ease;
    }

    a.back-link:hover {
        background-color: #1AAD19;
        color: #fff;
    }

    /* Simple Fade Up Animation */
    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(30px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>